/**
 * 비용 보호 / 안전장치 상수.
 *
 * 세션 1에서는 OpenAI API 비용이 폭증하지 않도록
 * 입력 길이 / 출력 토큰 / 모델을 모두 작은 값으로 제한한다.
 *
 * TODO SESSION 1-5 (선택): 본인 캐릭터의 응답 길이에 맞춰
 * MAX_OUTPUT_TOKENS를 조정해보세요. 너무 크게 잡으면 비용이 빠르게 늘어납니다.
 */

// 사용자가 한 번에 보낼 수 있는 메시지 최대 글자 수.
// 너무 길면 토큰 비용이 커지고, 모델 응답도 느려집니다.
export const MAX_INPUT_CHARS = 500;

// 모델이 한 번에 생성할 수 있는 최대 출력 토큰 수.
// 1 토큰 ≈ 영어 4글자 / 한글 1~2글자 정도로 생각하면 됩니다.
export const MAX_OUTPUT_TOKENS = 500;

// 사용할 모델 이름.
//
// 기본은 가장 저렴한 gpt-4o-mini입니다.
// TODO SESSION 1-6 (선택): 더 똑똑한 응답이 필요하면 "gpt-4o"로 바꿔보세요.
// 단, 비용이 약 15~20배 비싸집니다.
//
// 다른 후보:
//   - "gpt-4o-mini"  (저렴, 빠름, 기본값)
//   - "gpt-4o"       (똑똑함, 비쌈)
//   - "gpt-4.1-mini" (균형, 사용 가능 여부는 계정에 따라 다름)
export const MODEL_NAME = "gpt-4o-mini";

// 대화 히스토리에 포함할 최근 메시지 수.
// 너무 길면 매 요청마다 토큰을 많이 먹습니다.
export const MAX_HISTORY_MESSAGES = 20;
